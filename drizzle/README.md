# Drizzle Directory

This folder contains Drizzle schema migrations and the migration meta journal.

Migration files are generated by `pnpm db:generate` and applied by `pnpm db:push`. Keep schema definitions explicit and aligned with the documented action lifecycle.
